# uNews

**uNews** — Telegram-бот и система публикации новостей, патчноутов и отчётов разработки по u-проектам.

Проект предназначен для автоматической или полуавтоматической публикации обновлений из GitHub-репозиториев в Telegram-канал.

## Назначение

**uNews** помогает вести единый Telegram-канал с новостями разработки:

- публиковать патчноуты;
- публиковать отчёты о разработке;
- отправлять новости проектов;
- прикреплять изображения к каждому посту;
- хранить историю обновлений в GitHub;
- использовать GitHub Actions для автоматической публикации.

## Основные проекты

- uSugar
- uDream
- uChurch
- GOART
- Time Rift
- другие проекты автора

## Как работает система

1. В папку `news/` добавляется файл патчноута.
2. Рядом добавляется изображение с таким же именем.
3. GitHub Actions запускает скрипт публикации.
4. Скрипт отправляет пост в Telegram-канал.
5. Telegram-канал получает полноценный патчноут с картинкой.

## Публикация патчноута

Скрипт публикации принимает путь к `.md`-файлу патчноута:

```bash
npm run publish:projects:check -- ../500_td_game/news/2026-06-14-500td-v1-0-2-pages-preview.md
```

Для реальной отправки нужны переменные окружения:

```bash
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHANNEL_ID=...
```

Если в YAML-блоке есть поле `images`, порядок изображений берётся строго из этого списка.
При нескольких изображениях используется Telegram `sendMediaGroup`: первое изображение получает caption, остальные отправляются без caption.
Если `images` нет, используется старое поле `image` и отправка через `sendPhoto`.
Если изображений нет, отправляется текст через `sendMessage`.

## Пример структуры патчноута

```text
news/
├── 2026-06-14-usugar-settings.md
└── 2026-06-14-usugar-settings.png
Статус проекта

Проект находится на этапе проектирования и первичной настройки.


Для Telegram-канала лучше так:

```text
uNews — новости u-проектов

Описание канала:

Новости разработки, патчноуты, отчёты обновлений и заметки по проектам uSugar, uDream, uChurch и другим u-проектам.
